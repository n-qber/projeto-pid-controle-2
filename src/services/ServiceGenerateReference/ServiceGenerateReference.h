#ifndef SERVICEGENERATEREFERENCE_H
#define SERVICEGENERATEREFERENCE_H

/**
 * @brief This service is responsible for generating the reference signal.
 */
class ServiceGenerateReference {
public:
    // This service has no constructor.
    
    /**
     * @brief Generates the reference signal.
     */
    void generate_reference();
};

#endif // SERVICEGENERATEREFERENCE_H
