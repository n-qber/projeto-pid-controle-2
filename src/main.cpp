#include "services/majors/ServiceScheduler/ServiceScheduler.h"

int main() {
    // Create the main scheduler instance
    ServiceScheduler scheduler;

    // Run the system
    scheduler.run();

    return 0; // This line will likely never be reached
}